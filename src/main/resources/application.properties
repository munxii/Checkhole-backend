spring.application.name=ShinkHole

# Load local .env (properties format) if present — for JWT_SECRET, MONGODB_URI, etc.
spring.config.import=optional:file:./.env[.properties]

spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://eun_xii:2VrnYzMuSL33n3dR@cluster0.igucljc.mongodb.net/shinkhole?retryWrites=true&w=majority}
spring.data.mongodb.database=shinkhole

jwt.secret=${JWT_SECRET}
